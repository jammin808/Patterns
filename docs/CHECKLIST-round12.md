# Round 12 — Windows checklist

The headless suite proves the logic on every push. These are the things only a real rig shows:
one line per item, what to do, and what you should see. Tick them on a Windows machine with the
full build, two displays, an audio interface and, where named, an NDI receiver, a stream
destination, a Stream Deck with Companion and an OSC controller.

| # | Item | Do | Expect |
| --- | --- | --- | --- |
| 1 | Lower thirds: air, preview, update, the default | With EDIT SAFE on (the default), two displays, an NDI send and the stream running: Lower thirds page → + New (Neon), type a name, press AIR; change the name's spelling in Name shown; press AIR again; change it again and press UPDATE ON AIR; press TAKE on the switcher while it is on; switch EDIT SAFE off and on; + New (Clean), star it (★), + Person twice in the LIBRARY, press PVW on a person, watch the PREVIEW pane and a Multiview display's Preview tile, press REVIEW on the Show panel, then TAKE TO AIR; with the design on air press a PEOPLE chip on the Show panel; HIDE; press a PEOPLE chip again; on the phone's LOWER THIRDS tab press PVW FIRST, a design, a person, TAKE TO AIR; from a Stream Deck LT PVW 1, PVW PERSON 2, LT TAKE, LT UPDATE; from a TCP client `LT PREVIEW 1 WITH 2`, `LT TAKE`, `LT UPDATE`, `PERSON 1`; over OSC `/patterns/lowerthird/preview/1/2`, `/patterns/lowerthird/take`; add a cue with Lower third to preview and a person and the next with Lower third take, GO through them; save and reopen the show | The design arrives on both displays, the NDI receiver and the stream at once; the first respelling does **not** reach the outputs and the page lights EDITED with UPDATE ON AIR beside it (the old build stayed like this for good); AIR again puts the respelt name on every output, arriving afresh; UPDATE ON AIR changes the words on every output without the design leaving; the picture TAKE changes the picture under it while the lower third stays exactly where it was (no re-entry, no drop); EDIT SAFE off and on leaves it alone too; PVW puts the person into the design on the PREVIEW pane and the Preview tile — labelled IN PREVIEW on the page, amber on the Show panel — while the outputs show nothing new, REVIEW fills the multiview with it, TAKE TO AIR puts it on every output and the preview clears; the PEOPLE chip changes the name in the design on air; after HIDE the chip puts the person into the starred design, not the one last shown; the phone's chips go amber in the preview and its TAKE TO AIR puts them on; the Stream Deck's LT TAKE key lights amber while something is in the preview and LT UPDATE while the air copy is behind, and both work; the TCP lines and OSC do the same and a stranger's name answers `ERR`; the two cues run the sign-off in two GOs; the reopened show has the ★ where it was and nothing in the preview |
