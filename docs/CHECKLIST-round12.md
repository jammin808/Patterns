# Round 12 — Windows checklist

The headless suite proves the logic on every push. These are the things only a real rig shows:
one line per item, what to do, and what you should see. Tick them on a Windows machine with the
full build, two displays, an audio interface and, where named, an NDI receiver, a stream
destination, a Stream Deck with Companion and an OSC controller.

| # | Item | Do | Expect |
| --- | --- | --- | --- |
| 9 | The Interactive area — an Arduino and a Pi | Flash the sketch in docs/ARDUINO.md onto an Arduino with two buttons and two LEDs; SETUP → Interactive → + ARDUINO (SERIAL), type its COM port (the page lists the machine's ports), 115200, LF; switch the area on; press BTN1 with the stack armed and a cue on standby, then with the stack disarmed; press BTN2; press BLACKOUT on the desk, then lift it; ARM and disarm the stack; type PING in the SEND box and press SEND; add a cue with Device — send a line, target Arduino, value RELAY 1, GO it; send `DEVICE Arduino RELAY 0` from a TCP client and `/patterns/device/Arduino "RELAY 1"` over OSC; press a Companion key with `device_send`; unplug the Arduino, watch the card, plug it back; run the Pi script on a Raspberry Pi, + DEVICE OVER IP with its address and port 7000, TCP; press the Pi's button; stop the script and start it again; switch the area off | The card reads open (COM3 at 115200) within a second and the board's LEDs take the show's state at once (BLACKOUT off, ARMED as the stack is); BTN1 GOes the standby cue — the journal reads "GO from device Arduino" — and, disarmed, the board hears ERR and the reason while nothing fires; BTN2 steps the presenter; the BLACKOUT LED follows the desk both ways and the ARMED LED the stack; PING is answered PONG in the card's last line in; the cue's RELAY 1 and the wire's, OSC's and Companion's lines each reach the board (the card's last line out reads them); unplugged, the card reads closed and retrying and the show runs on; plugged back, it reopens by itself and hears every fact again; the Pi's card reads open, its button fires its trigger, its lamp follows the blackout; with the script stopped the card reads reconnecting and open again within two seconds of the restart; off, every card reads Interactive area off and a cue's line is refused with the reason |
| 8 | Companion 2.0 banks and OSC feedback | Install the 2.0.0 module (`yarn && yarn package`, re-add the dev module), connect; drag a row of eight *Look bank* keys and a row of *Cue bank* keys onto a page, plus two *Lower thirds* keys and a *Screens* key; in Patterns save three looks, rename the second, put the first on F1, delete the third, save a fourth; recall a look from the desk and from a bank key; with EDIT SAFE on, press → PVW on a look; build a cue stack of five cues, ARM with the second on standby, press cue bank key 3, then GO; open a second show; open the module's presets list and look at *Looks — this show*; on TouchOSC bind a label to `/patterns/state/looks/2` and a button to `/patterns/look/index/2` | The bank keys light up with the looks' names as each is saved — the third key dims again when its look is deleted and lights with the fourth — and the second key's text changes with the rename; the F1 key under *Looks* reads F1 and the first look's name; the key whose look is on air is green from the desk recall as from the bank key, the key whose look is in the preview amber; the cue bank reads the standby cue on its first key (green) and the next six after it, cue bank key 3 moves the standby there and GO fires it, after which the keys relabel; the lower-third keys read the designs' names and the screen key the screen's label, green while enabled; the second show's names replace the first's on every key within a second; *Looks — this show* lists one preset per look of the loaded show and changes when the show does; the TouchOSC label reads the second look's name and the button applies it |
| 7 | Edge blend beyond two projectors | With three projectors on one wall (or three displays standing in): Screens page, tick Automatic on all three, drag P2 over P1 by the overlap and P3 over P2 by the same; select P2 and read the lines under Edge blend; untick Automatic on P3 and read P2's and P3's lines; tick it again and set P3's curve to Linear; put it back; Pattern page → Projection blend, Projectors 3, the real overlap, OUTPUTS ON; tick the 50% grey check and turn each projector's Blend gamma; with four projectors in a 2×2 (or a 2×2 of displays): overlap them both ways, select each and read its lines, choose the Blend 2×2 WUXGA preset from the Library and set Rows across 2 with the real overlaps; put a photo on the canvas | The three join into one canvas the size of the union; P2's lines read ✓ Left join with P1: n px, both fade, SCurve and ✓ Right join with P3 …; with P3 not fading, P2 reads ⚠ … P3 does not fade its left edge — the join will be bright and P3 reads ⚠ … tick Automatic, or type n in Left px; with the curve differing P2 reads ⚠ … use the same curve on both; the pattern's P1 / P2 / P3 frames land on the projectors, both overlap zones hatched with OVERLAP n px and the ramps opposing; the grey check reads flat across both joins once the gammas are right (no bright or dark band); in the 2×2 every projector reads a side join and a top or bottom join, the pattern shows four frames with zones both ways and the chip reads 2×2 · overlap n/m px, the corner where all four meet reads as flat as the rest on the grey check, and the photo shows as one picture with no seam, no double-bright corner |
| 6 | PowerPoint through LibreOffice | With LibreOffice installed (libreoffice.org, the default folder): Media page → Source: Deck, Browse… a .pptx with a dozen slides, pictures and a font the machine lacks; watch the PREVIEW pane and the readout under the file box; when the pages arrive press NEXT PAGE and the clicker's keys; save the pattern as a look and recall it after a restart of Patterns; edit the .pptx in PowerPoint (add a slide), save it, recall the look again; press RELOAD; uninstall or rename LibreOffice's folder, restart Patterns, recall the look; type the path to soffice.exe (or its folder) in the LibreOffice path box; put a LibreOfficePortable folder beside Patterns.exe on a machine with no install; open PowerPoint on the same machine with the deck open and recall the look; send STATE from a TCP client while a conversion runs; pick the .pptx from the Library's Decks section | The card reads *Converting name with LibreOffice Impress…* for a few seconds and the line above says where LibreOffice was found; the pages then arrive on the PREVIEW pane and every output, sharp, at the slides' shape, and the click-through works as for a PDF; the recalled look opens at once from the cache (no LibreOffice run — decks/ beside the show holds the PDF); the edited deck converts again by itself and shows the new slide; RELOAD converts afresh; without LibreOffice the card and the readout say *LibreOffice not found — install it… or export the deck as PDF* and the LibreOffice path box appears; the typed path is found at once (no restart) and the deck converts; the portable copy is found by itself; a PowerPoint window open on the machine neither blocks nor is touched by the conversion; STATE's deck row reads `"converting": true`, `"kind": "PowerPoint"`, count 0 and the status, then the pages; the library tile puts the .pptx on the pattern as a deck; the substituted font is visible on the slide (the known limit, for the graphics op to check before the show) |
| 5 | PDF decks and the click-through | Export a 4:3 deck from PowerPoint as PDF; Media page → Source: Deck, Browse… the PDF; OUTPUTS ON; press NEXT PAGE and Previous on the page; press the clicker's Page Down / Page Up with no clicker list armed; on the phone's SHOW tab press Next / Back; from Companion press DECK ▶; save the pattern as a look, add a cue that applies it and a second cue that applies another look, ARM the stack with the second cue on standby, GO the first cue, click through to the last page and click once more; tick off "At the last page, the next click GOes…" and repeat; send DECK PAGE 2 and DECK LAST from a TCP client; set Start at page 3 and recall the look again; pick the PDF from the Library's Decks section | The page shows full frame with bars either side on a 16:9 screen, sharp on every output, the NDI send and the stream; NEXT PAGE, the clicker's keys, the phone and Companion all turn the pages, and the readout under the file box and the PREVIEW header read Page n / N; on the last page the header says the next click GOes the standby cue and the Companion DECK ▶ key turns amber; the next click fires the second cue and its look replaces the deck; with the tick off the deck holds its last page and the status says nothing follows; DECK PAGE 2 and DECK LAST land on those pages; the recalled look opens at page 3; the library tile puts the PDF on the pattern as a deck |
| 4 | Web pages that answer | Remote & web page: paste a YouTube watch link, SHOW THIS PAGE ON THE PATTERN; on the Media page press PLAY / PAUSE, MUTE, RESTART under PAGE CONTROLS; click the video on the PREVIEW pane; paste a Google Slides link (a published one, or your own) in the address box, press FULL FRAME, then NEXT, PREVIOUS, BLACK, PRESENT; press ⌨ KEYS → PAGE, press F5, the arrows and Space, then Ctrl+Alt+K and Space again; on a plain page with a search box, KEYS → PAGE and type a word; on the phone's SHOW tab press the PAGE ON AIR buttons; from Companion's Web page presets press PAGE NEXT; add a cue with Web page — key or action, value present, and another with next, GO them; send WEB KEY dance from a TCP client | The service line under the address names YouTube and the pattern shows the player alone with no controls; PLAY / PAUSE, MUTE and RESTART act on the video at once (through the player, whether or not the page was clicked); a click on the PREVIEW pane lands where it looks; the Slides deck shows alone and NEXT / PREVIOUS move the slides, BLACK blanks, PRESENT starts your own deck from its editor; with KEYS → PAGE on, F5, the arrows and Space go to the page and the desk's F5 look and Space blackout do not fire, and after Ctrl+Alt+K Space is the blackout again; typing arrives in the search box; the phone shows the service, the page and its buttons and each one acts; the Companion key lights blue while a page is on air and PAGE NEXT moves the slide; the cues read Page: present and Page: next slide and act on GO; WEB KEY dance answers ERR naming the actions; no button opens a browser window anywhere |
| 3 | The area of interest | Media page: a Web page with a Teams or Google Slides address (or any still), Fit; press PICK ON PREVIEW and drag a box on the PREVIEW pane around the shared picture; press PICK ON PREVIEW again and drag a box inside the picture as it shows now; press Top bar off, Side panel off, Centre 80%, then Clear; move Cut right to 30; tick Mirror; set Turn to 90° clockwise; save the pattern as a look, recall another look, recall this one; watch an output, an NDI send and the stream throughout | The box becomes the picture on the PREVIEW pane, on every output, the NDI send and the stream at the same moment, fitted to the target with nothing outside it; the second box narrows further (a box within the first, not a fresh one); the summary line reads Keeps n% × n% of the picture (from n% in, n% down); each preset reads in the sliders and Clear returns The whole picture; the slider crops as it moves; Mirror swaps left and right, the 90° turn stands the picture on its side fitted at its new shape and the summary adds Turned 90° and Mirrored; a click on the page lands where it looks like it lands while the page is upright and does nothing while it is turned; the look carries the crop, the mirror and the turn back |
| 2 | The multiview's tally | Pattern page: make display 2 a Multiview with tiles Program, Screen (display 1), Screen (a joined canvas if you have one, else the NDI sender's own screen), Preview, an NDI feed and the clock; OUTPUTS ON; with EDIT SAFE on, un-arm display 1's tile on the switcher strip, then arm it again; lock it (the padlock), unlock it; give it its own picture (OWN) and take it off; press BLACKOUT, lift it; press FREEZE, release it; OUTPUTS OFF; EDIT SAFE off; watch /multiview in a browser and the phone's SCREENS tab meanwhile; switch the tally checkbox off | Every tile carries its badges along its top edge and a two-part bar underneath — the name on the left, on the right SCREEN 1 · 1920×1080 (or CANVAS A · … · 2 SCREENS, NDI SEND …); the PROGRAM tile reads ON 1 · A (the screens the program is on) and the PREVIEW tile NEXT TAKE → 1 · A; display 1's tile reads PGM with a red border and NEXT in green; un-armed it reads HELD in amber and the PREVIEW tile's list drops it; armed again NEXT is back; locked it reads LOCKED and leaves the PROGRAM tile's list; unlocked it reads OWN until the own picture is taken off; BLACKOUT turns the PGM chips into BLACK; FREEZE adds a cyan FROZEN chip; OUTPUTS OFF reads OUTPUTS OFF on every output tile; with EDIT SAFE off the Preview tile reads NO PREVIEW and the NEXT / HELD chips go; /multiview shows the same chips and words as the display, and the phone's SCREENS buttons read NEXT / HELD / OWN in step; with the checkbox off the chips and borders go and the captions stay |
| 1 | Lower thirds: air, preview, update, the default | With EDIT SAFE on (the default), two displays, an NDI send and the stream running: Lower thirds page → + New (Neon), type a name, press AIR; change the name's spelling in Name shown; press AIR again; change it again and press UPDATE ON AIR; press TAKE on the switcher while it is on; switch EDIT SAFE off and on; + New (Clean), star it (★), + Person twice in the LIBRARY, press PVW on a person, watch the PREVIEW pane and a Multiview display's Preview tile, press REVIEW on the Show panel, then TAKE TO AIR; with the design on air press a PEOPLE chip on the Show panel; HIDE; press a PEOPLE chip again; on the phone's LOWER THIRDS tab press PVW FIRST, a design, a person, TAKE TO AIR; from a Stream Deck LT PVW 1, PVW PERSON 2, LT TAKE, LT UPDATE; from a TCP client `LT PREVIEW 1 WITH 2`, `LT TAKE`, `LT UPDATE`, `PERSON 1`; over OSC `/patterns/lowerthird/preview/1/2`, `/patterns/lowerthird/take`; add a cue with Lower third to preview and a person and the next with Lower third take, GO through them; save and reopen the show | The design arrives on both displays, the NDI receiver and the stream at once; the first respelling does **not** reach the outputs and the page lights EDITED with UPDATE ON AIR beside it (the old build stayed like this for good); AIR again puts the respelt name on every output, arriving afresh; UPDATE ON AIR changes the words on every output without the design leaving; the picture TAKE changes the picture under it while the lower third stays exactly where it was (no re-entry, no drop); EDIT SAFE off and on leaves it alone too; PVW puts the person into the design on the PREVIEW pane and the Preview tile — labelled IN PREVIEW on the page, amber on the Show panel — while the outputs show nothing new, REVIEW fills the multiview with it, TAKE TO AIR puts it on every output and the preview clears; the PEOPLE chip changes the name in the design on air; after HIDE the chip puts the person into the starred design, not the one last shown; the phone's chips go amber in the preview and its TAKE TO AIR puts them on; the Stream Deck's LT TAKE key lights amber while something is in the preview and LT UPDATE while the air copy is behind, and both work; the TCP lines and OSC do the same and a stranger's name answers `ERR`; the two cues run the sign-off in two GOs; the reopened show has the ★ where it was and nothing in the preview |
