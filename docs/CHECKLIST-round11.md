# Round 11 — Windows checklist

The headless suite proves the logic on every push. These are the things only a real rig shows:
one line per item, what to do, and what you should see. Tick them on a Windows machine with the
full build, two displays, an audio interface and, where named, an NDI receiver, a stream
destination, a Stream Deck with Companion and an OSC controller.

| # | Item | Do | Expect |
| --- | --- | --- | --- |
| 10 | Review on the multiview | Pattern page: make the second display a Multiview with a Program tile and a Preview tile; with EDIT SAFE on, change the preview's pattern; press REVIEW on the Show panel, then again; press it on the phone's SHOW tab and a Stream Deck REVIEW key; send `REVIEW ON` from a TCP client and `/patterns/review 0` over OSC; watch /multiview in a browser meanwhile; switch EDIT SAFE off; save and reopen the show | The Preview tile shows the edited picture beside the unchanged program tile, labelled PREVIEW, with no tally; REVIEW fills the whole multiview display — and /multiview, and an NDI send of it — with the preview under a REVIEW · PREVIEW chip while the audience's screens keep the program, and the tiles come back when it is off; the phone's button and the Stream Deck key light green while it is on and follow a change made anywhere else; the TCP line and the OSC message flip it; with EDIT SAFE off the Preview tile reads a slate and REVIEW shows the slate; the reopened show has review off |
| 9 | The phone remote | On a phone open the remote's address; swipe the menu, open CUES, ARM, press GO and HOLD; on SCREENS tap a screen and its padlock; on AUDIO fire a VOG and stop it; on LOWER THIRDS put a design on and a person into it; on SETUP read the lines and open the caller's page and the multiview; press STOP ALL once, then twice; close the browser and reopen the address; put the phone in a pocket for a minute and take it out; switch off Wi-Fi on the phone | The header names what is on air and its chips light as the desk changes (within a blink, not on a timer); GO fires the standby cue, HOLD lights amber, ARM reads DISARM while armed and the desk shows the arm; the screen goes off and its padlock turns amber locked; the VOG lights while it plays and the stop button appears on SHOW and AUDIO; the design lights and the person's name appears on the hide button; SETUP reads the health line, the CPU/RAM/fps, the stream and the beacon words; the first STOP ALL press reads PRESS AGAIN and the second stops everything but the picture; the page reopens on the tab it was on; after the pocket it catches up at once; without Wi-Fi the dot goes red and Connection lost — retrying… shows, and it reconnects by itself |
| 8 | The watchdog and the beacon | Two machines on one network with the same show file: on the main, Machine page → tick Send a heartbeat beacon (leave the broadcast address); on the backup tick Listen; on the backup press SUPER-CHECK, then unplug the main's network cable for ten seconds and plug it back; on the main, start the stream to a dead URL and wait; put a Motion pattern on air and, with outputs open, suspend Patterns.exe in Process Explorer for 40 s and resume it; force a crash loop (a bad GPU choice, or kill the child seven times in ten minutes) and watch the backup, then start the main again by hand | The backup's health line reads Main machine MAIN seen 1 s ago: live · … and the super-check a green Main machine row; unplugged, within six seconds it reads MAIN MACHINE MAIN SILENT for 6 s — take over? (the row red), and seen again when the cable is back; the main's advice list shows The stream stopped by itself with the encoder's reason; after the suspend the advice list shows Outputs are open but drew no frames for 30 s (the heartbeat alone would not have caught it) and clears once frames flow; when the watchdog gives up the backup reads its watchdog gave up — take over?, and the main's next start puts The watchdog gave up at HH:MM … on its health line once |
| 7 | OSC in and out | Remote page: tick OSC in, set Feedback to the tablet's address on 9699; on the tablet (TouchOSC, sending to the machine on 9698) press buttons for /patterns/blackout 1, /patterns/look 2, /patterns/screen/2 0, /patterns/lowerthird/1 with a person's name as the argument, /patterns/cue/go, /patterns/nonsense and /patterns/ping; move a fader on /patterns/music/volume; in QLab fire a Network cue with /patterns/blackout 0; watch the Remote page's OSC line and the journal; untick OSC in | Each button does what the TCP line would (the screens go black, the look recalls, screen 2 goes off, the lower third arrives with that person, the standby cue fires), the tablet's LEDs on /patterns/state/blackout, /screen/2 and /lowerthird follow within a blink, the fader sets the break-music level in percent, /patterns/nonsense answers /patterns/error to the tablet and nothing changes, ping answers pong, QLab lifts the blackout; the OSC line reads the port, the feedback address, the counts and the last message, the journal reads "from osc 10.0.0.5:…"; unticked, the port closes and the tablet's messages do nothing |
| 6 | The lower-thirds library | Lower thirds page: Template CSV, fill six speakers in Excel with roles, companies and headshot paths, save as .xlsx and IMPORT LIST; Append a CSV that changes one role; select the Headshot design, USE on one person, then SHOW on another; Show panel: press a PEOPLE chip while a design is on; Cues page: Lower third on with the design and a person, GO; phone remote: PEOPLE buttons; Stream Deck: a PERSON key; from a TCP client send `PERSON nobody` | The list becomes the library with names, roles and companies; the appended row updates the person without doubling; USE puts the name, role and company into the design's fields and the headshot into the Photo element on the stage, SHOW does the same and the design arrives on every output with that person; the chip puts the next person into the design on air and it arrives again; the cue shows the design with the person and reads "Lower third 'Headshot' — Jane Doe"; the phone's PEOPLE buttons light the person on screen; the Stream Deck key lights while its person is on; the TCP client reads ERR … not in the lower-thirds library and nothing on screen changes |
| 5 | The caller's home | Cues page: Template CSV, fill six rows in Excel with starts, lengths, a coffee break, a lunch, a look by name and one action, save as .xlsx and IMPORT SHEET; Append a two-row CSV; give a cue a look from the Quick pick and press + Countdown; set Follow 0 on one cue and 5 s on the next; RUN: ARM, GO late on purpose, read the LIVE strip and the line above the list, press +1 MIN, RESUME NOW and CATCH UP; GO the follow cues; press HOLD during a follow's countdown; Export CSV and open it in Excel | The sheet becomes the list with its numbers, times, marks and the look resolved, the report naming anything not found; the appended rows continue the numbering; the Quick pick sets the cue's Apply look and the press adds the action with its field to fill; the LIVE strip reads n MIN LATE in amber, the marks line says when coffee, lunch and the end are expected with the lateness, every row shows planned → expected, +1 MIN moves the later starts, RESUME NOW plans the standby cue for the clock, CATCH UP takes the lateness off the lengths before coffee and says so; the follow-0 cue fires the next at once and the 5 s one counts down on the standby card and fires by itself, and HOLD stops one; the export opens in Excel with the same columns |
| 4 | Web pages in the engine | Media page: source Web page, the address of a live dashboard, 1920 × 1080, zoom 125 %, 'Show the pointer' on; on the PREVIEW pane click a link, drag a slider, scroll with the wheel; type into a search box with PAGE CONTROLS and press Enter; put a second page on LAYER 2 and Alt-drag its box, then click inside it without Alt; set the pattern on a two-display span with an NDI send and the stream running; save a look, change the page, recall the look; turn 'Show the pointer' off; move WebView2Loader.dll away and restart | The page appears on the PREVIEW and the outputs within a few seconds, with moving content at a steady rate and its sound following Mute; every click lands where the pointer is and the room sees the arrow and a ripple at that spot on every output, the PROGRAM pane and the multiview; the wheel scrolls, the typed text arrives in the box and Enter submits; the layer's box moves with Alt and its page still takes the click without; the span's two displays, the NDI receiver and the stream all show the page; the look brings the page, its size and zoom back with one fade; with the pointer off nothing is drawn over the page; without the loader the pattern shows a card naming what is missing instead of a blank, and the log says so once |
| 3 | Layers and dragging | Media page: switch LAYER 1 on with a still, LAYER 2 on with an NDI feed cropped 10 % each side with a 12 px white border and 24 px corners; drag both on the PREVIEW pane, then the clock and the PiP inset; set LAYER 1's source to another screen; TAKE; save a look and recall it; Lower thirds page: drag the name element on the stage | The still and the feed sit in their boxes over the pattern on both displays of a span, the NDI receiver and the stream, cropped, bordered and rounded; each box follows the pointer on the pane with no crossfade, an empty layer shows a dashed box to place; the clock and the inset move from their anchors and the Nudge sliders read the same numbers; the screen layer shows the other display's picture live; TAKE puts it all on air with one fade; the look brings the layers back where they were; the dragged element moves in the designer and its X / Y fields follow |
| 2 | Screens with a job | Two displays: on the Screens page set the second to Confidence, then on the wall build a look in EDIT SAFE and TAKE; recall an F-key look; fire a stinger clip; press SEND on the confidence tile; add a cue with Screen unlock for it and GO; on a Stream Deck press the `screen_lock` key; set the second display to Repeater with Mirror of the first, then give the first its own pattern (OWN) and change it | The confidence display keeps the picture it had through the TAKE, the look and the clip while the main display changes, its tile wears CONF and an amber LOCK and reads held while the send is built; SEND puts the preview on it alone; after the cue's unlock the next look reaches it; the Stream Deck key lights amber while it is locked; as a repeater it shows the first display's picture and follows every change to it, and its tile says ↳ and what it repeats |
| 1 | The room, the type, the modes | Open a show saved by an older build; press ? TIPS on the Screens page, tick Show hints on the pages, save, restart; press PREP with the outputs open, an NDI send running and the stream on; press RUN, then PREP | The pages open without their explanations and every control sits a little larger — the header, the rail, the chips, the numeric fields — with nothing off the window at 1180 × 720; ? TIPS lists the Screens page's explanations under their headings and the tick puts them back on every page; after the restart they are still on; PREP closes the windows, the receiver loses the send and the stream's status says PREP — held (the destination sees it stop), and SHOW brings the send and the stream back by themselves; the header reads MODE over PREP · SHOW and LAYOUT over RUN, RUN lights alone, and PREP from the Run surface leaves it first |
