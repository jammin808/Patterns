# Round 11 — Windows checklist

The headless suite proves the logic on every push. These are the things only a real rig shows:
one line per item, what to do, and what you should see. Tick them on a Windows machine with the
full build, two displays, an audio interface and, where named, an NDI receiver, a stream
destination, a Stream Deck with Companion and an OSC controller.

| # | Item | Do | Expect |
| --- | --- | --- | --- |
| 3 | Layers and dragging | Media page: switch LAYER 1 on with a still, LAYER 2 on with an NDI feed cropped 10 % each side with a 12 px white border and 24 px corners; drag both on the PREVIEW pane, then the clock and the PiP inset; set LAYER 1's source to another screen; TAKE; save a look and recall it; Lower thirds page: drag the name element on the stage | The still and the feed sit in their boxes over the pattern on both displays of a span, the NDI receiver and the stream, cropped, bordered and rounded; each box follows the pointer on the pane with no crossfade, an empty layer shows a dashed box to place; the clock and the inset move from their anchors and the Nudge sliders read the same numbers; the screen layer shows the other display's picture live; TAKE puts it all on air with one fade; the look brings the layers back where they were; the dragged element moves in the designer and its X / Y fields follow |
| 2 | Screens with a job | Two displays: on the Screens page set the second to Confidence, then on the wall build a look in EDIT SAFE and TAKE; recall an F-key look; fire a stinger clip; press SEND on the confidence tile; add a cue with Screen unlock for it and GO; on a Stream Deck press the `screen_lock` key; set the second display to Repeater with Mirror of the first, then give the first its own pattern (OWN) and change it | The confidence display keeps the picture it had through the TAKE, the look and the clip while the main display changes, its tile wears CONF and an amber LOCK and reads held while the send is built; SEND puts the preview on it alone; after the cue's unlock the next look reaches it; the Stream Deck key lights amber while it is locked; as a repeater it shows the first display's picture and follows every change to it, and its tile says ↳ and what it repeats |
| 1 | The room, the type, the modes | Open a show saved by an older build; press ? TIPS on the Screens page, tick Show hints on the pages, save, restart; press PREP with the outputs open, an NDI send running and the stream on; press RUN, then PREP | The pages open without their explanations and every control sits a little larger — the header, the rail, the chips, the numeric fields — with nothing off the window at 1180 × 720; ? TIPS lists the Screens page's explanations under their headings and the tick puts them back on every page; after the restart they are still on; PREP closes the windows, the receiver loses the send and the stream's status says PREP — held (the destination sees it stop), and SHOW brings the send and the stream back by themselves; the header reads MODE over PREP · SHOW and LAYOUT over RUN, RUN lights alone, and PREP from the Run surface leaves it first |
