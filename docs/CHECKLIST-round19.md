# Round 19 — Windows checklist

The headless suite proves the logic on every push. These are the things only a real rig shows:
one line per item, what to do, and what you should see. Tick them on a Windows machine with the
full build (libVLC bundled), two displays, an internet connection and an Anthropic API key for
the assistant rows.

| # | Item | Do | Expect |
| --- | --- | --- | --- |
| 1 | The assistant: newest first, the preview only, screens apart | BUILD → Assistant with a key saved: ask two questions in a row; with a look on air and EDIT SAFE off, APPLY a look proposal; press TAKE; with a screen tile set to OWN and selected as the editing target, APPLY another look proposal; ask for a plan with three screens and APPLY the screens; SETUP → Screens | The latest answer sits under the ask box with a lit border and the history runs down the page newest first; the outputs keep their picture through the APPLY while the PREVIEW pane shows the new look and EDIT SAFE lights, the chip's applied line ends *TAKE or CUT puts it on air*; TAKE puts it on air; the second APPLY switches the editing target back to Program before it lands; the three planned screens are three separate tiles on the wall and on the Screens page — no *Canvas A* — with a gap between them, and dragging one flush against another joins them as before |
