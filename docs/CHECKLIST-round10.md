# Round 10 — Windows checklist

The headless suite proves the logic on every push. These are the things only a real rig shows:
one line per item, what to do, and what you should see. Tick them on a Windows machine with the
full build, two displays, an audio interface and, where named, an NDI receiver and a stream
destination.

| # | Item | Do | Expect |
| --- | --- | --- | --- |
| 8 | Lower thirds on the rig | Lower thirds page: make one of each preset, show each on a joined canvas of two displays with an NDI send and the stream running, fire an effect sting (a Supernova) while Neon and Sparks are up, hide each | Every design arrives on the show's clock — the same frame of the animation on both halves of the span, on the NDI receiver and on the stream — sits in its box at the same size on a 1080 and a 4K display, and leaves cleanly; the Neon frame's chaser runs round the edge and its glow brightens with the sting, the Sparks burst with it; Corporate shows the brand logo and company; Stamp carries today's date and the time of day top right; a Media element's clip plays silently under the words and stops when the design leaves; nothing else on screen changes |
| 7 | Direct output | Screens page: select a display, tick Direct output, save, restart Patterns, OUTPUTS ON; run PresentMon (or the Xbox Game Bar's performance overlay) against Patterns.exe; drag another app's window over the output, then away; from Task Manager kill Patterns.exe during a start, before the desk shows, and start it again; untick and tick, save, restart | Before the restart the line under the tick says Restart Patterns to take effect; after it, DIRECT with the swap chain in force; PresentMon shows the output's present mode as Hardware: Independent Flip, Composed: Flip while the other window sits on it and Independent Flip again when it leaves; the picture, blend, warp and trims are unchanged and the desk window looks as before; the Machine page's Direct line and the super-check's GRAPHICS row say 1 output asks · in force; after the killed start the next start composes with Held off on the line and the desk comes up; after the re-tick and the restart it is DIRECT again |
| 6 | Master clock, lock and lip-sync | Audio page: play a long track on the computer output and an HDMI display's output together, leave it two hours, watch MASTER CLOCK & SYNC; untick "Lock every output to the master clock" for ten minutes; put a clip on air and drag "Video sound delay"; drag a device row's delay; Stream page: set an audio delay and START STREAM; press SYNC CHECK with the outputs, the NDI receiver and the stream up, then hold a phone's slow-motion camera on a display beside a speaker | Each output's line shows its clock's ppm within the first minute, a correction of the same size the other way and a lag that stays within a few milliseconds all the way — the two outputs never phase; with the lock off the lag line grows steadily (tens of milliseconds an hour on a cheap interface) and snaps back when the lock returns, with no click either way; the clip's soundtrack moves against the picture as the video delay moves and a device row's delay holds that output back by exactly the milliseconds shown, the fades and ducks unaffected; the stream's sound moves against its picture by the delay after the restart; with SYNC CHECK on every display, the NDI picture and the stream flash white together every two seconds and the computer output clicks with the flash on the displays — the frames between a far-end flash and its click are the delay to set |
| 5 | Feed screens | NDI page: add a send, set it to "Its own screen", give that screen its own pattern on the wall (OWN) and TAKE a different look to it; set another send to mirror a joined canvas of two displays; Stream page: set the source to "Its own screen", START STREAM to a test RTMP endpoint | The send's screen appears on the Screens page (FEED SCREENS) and the wall, and the NDI receiver shows the own look while the displays show the program; the mirror of the canvas arrives at the canvas' shape with bars, not stretched; the stream shows the rendered own screen (its status says "rendered") at the stream's size and rate with the same encode; switching the stream back to a display shows the desktop capture again and its screen disappears |
| 4 | The super-check | Machine page: RUN SUPER-CHECK with the outputs open, an NDI send running and the stream on; unplug the laptop and run it again; open patterns.supercheck.txt | The Machine page widens as it opens; every row has a light and a real value — the CPU by name, the memory, each display with its mode and refresh rate, the card that renders, the outputs' frame rate against the master, the send running, the stream's status, the audio devices, the remote's address; on battery the Power row turns red and the level drops with "running on battery"; the file beside the exe carries the same rows; Copy report pastes into an email |
| 3 | A desk that resizes | Drag the divider between the page and the screens both ways, drag the handle between PROGRAM and PREVIEW, press ◧ WIDE, save the show, restart, load it; shrink the window to its minimum | The page and the screens follow the divider and the panes follow the handle, smoothly, at the display's rate; WIDE reduces the screens to a strip with the wall still working; the layout comes back after the restart and the load; at the minimum window the page is held back and TAKE stays on screen |
| 2 | The tally | Recall a look with its F-key, edit the pattern, load another look with → PVW in EDIT SAFE, TAKE; fire a VOG sound, a sting clip, a held sting and an effect sting | The Looks page row and the Show panel chip of the recalled look turn red PROGRAM, then PROGRAM · EDITED after the edit; the → PVW look turns green PREVIEW and red after the TAKE; the Audio page row and the Show panel chip of whatever plays turn red with the seconds counting, HOLDING on the held frame, and a bar running down the effect sting's length; everything goes dark by itself when the sound, clip or sting ends |
| 1 | Sliders and effect stings | Audio page: drag a pulse's length slider, the live-duck fade, the sting fade, the stop fade and the hold limit; put Particles or a Fractal on air and fire each of the twelve shapes | Every slider's number follows the drag and survives a save; a pulse runs for the length shown; a vortex visibly spins the field and turns the fractal before letting go, a freeze holds the picture in slow motion then releases, a strobe hits eight times with the colours flipping, a rainbow turns the colours right round, a quake shakes the whole picture; every shape is back to the plain picture by its end, identical on both displays of a span and on NDI |
