# Round 17 — Windows checklist

The headless suite proves the logic on every push. These are the things only a real rig shows:
one line per item, what to do, and what you should see. Tick them on a Windows machine with the
full build (libVLC bundled), two displays and an audio interface. The round came from a rig day —
a crash between pages, the switcher's tiles, a send that lost the preview, a screen's picture
into the preview, the grid on the tiles, the assistant's first question, a branded test card — so
the rows are the desk in use.

| # | Item | Do | Expect |
| --- | --- | --- | --- |
| 8 | Groups of screens | With three screens, an NDI send and the stream set to its own screen: read every wall tile's foot line; pause over a foot line; click screen 2's foot line; on the Screens page set Role to Confidence, then Info, then Repeater with Mirror of screen 1; back on the wall click the NDI tile's foot line; search Help for *group* and for *NDI feed* | The foot lines read MAIN · 1920×1080, NDI · 1280×720 (the send's size), STREAM · …, and PGM its size alone; the tooltip says what the group is and where it is set; the click opens SETUP → Screens with screen 2 selected and the status line says its group is set there; every role change reads on the tile at once (CONF, INFO, then REP ↳ 1 · … with the badge), and the tile's LOCK follows Confidence and Info; the NDI tile's click lands on the feed screen; Help finds *Screen roles — the groups of screens* and the Switcher topic's "Groups, in both senses" |
| 7 | The Patterns badge | BUILD → Pattern: Colour bars, then Grid, with two outputs, an NDI send and the stream on; look at every output and the wall's tiles; drag the badge on the PREVIEW pane; BUILD → Branding → PATTERNS BADGE: move it to the top right, height 14 %, type a stand number on the line, untick the line, tick *On media too* and put a video on the Media page; save a look with the badge off and recall it, then a look with it on; OVERLAYS OFF from the Show panel; open a show file saved before this build | Every output, the NDI send, the stream and the tiles show the card centred in the middle of the lower third — the test-card icon, PATTERNS, the magenta rule and the line — crisp at 4K and HD, in the app's cyan and magenta whatever the brand kit's colours; the drag moves it and the Nudge sliders follow; every change on the page shows live; the video carries it only with *On media too*; the recalled look turns it off and the next brings it back; OVERLAYS OFF clears it with the clock and the rest; the old show opens with it on |
| 6 | Any screen into the preview | With EDIT SAFE on: give screen 2 its own picture (OWN on its tile, edit, TAKE), edit something else in the preview, press → PVW on screen 2's tile; then → PVW on a screen that follows the program; SEND the preview to screen 3; switch EDIT SAFE off and press → PVW on screen 2 | The PREVIEW pane and the editors take screen 2's picture with the EDITING banner on PROGRAM and the PGM tile selected, the outputs do not change, the status reads *2 · …'s picture is in the preview — edit it, then SEND it to a screen or TAKE*; the screen on the program loads the program's picture, not the preview's edit; SEND lands it on screen 3 and the preview keeps it; with EDIT SAFE off the press opens EDIT SAFE first (the status says so) and nothing goes live; the SEND button shows on the screen tiles only while EDIT SAFE is on, → PVW always and never on PGM, both on the row under OWN / MON / ARM / LOCK |
| 5 | A send keeps the preview | BUILD → Pattern with EDIT SAFE on and outputs on: build a Grid, press SEND on screen 2's tile; change the cell size, press SEND on screen 3's tile; tick screen 1, SEND TO TICKED; then TAKE | After every SEND the PREVIEW pane and the PGM tile's PVW keep the picture you built — no jump back to what the program shows — and the sent screen's PGM miniature shows it with OWN lit, every other screen unchanged, the status line ending *the preview keeps the picture*; the second send lands the changed grid on screen 3 while screen 2 keeps the first; SEND TO TICKED does the same for screen 1 and clears the tick; TAKE puts the preview on the program while screens 1, 2 and 3 keep their own; EDIT SAFE is still on after each send and after the TAKE |
| 4 | The switcher's tiles | On the Build layout with three or more screens, maximise the window, then plug a display in or enable a screen so the tiles rebuild, and look at every tile; press ▸ at the end of a screen tile's title row, then ▾ on its bar; ▸ on PGM; save, close and reopen the show; SHOW → Run, ▸ COLLAPSE TILES then ◂ EXPAND TILES; on SETUP → Screens drag a screen flush against another, then apart | Every tile shows one face — the title row, the two miniatures, OWN / MON / ARM / LOCK — with the buttons at the top of the tile level with PGM's, and no name on its side over a body; ▸ makes that one tile a vertical bar (the tally, the name on its side, ▾) with the rest full, ▾ opens it again, PGM collapses the same way; the collapsed tiles come back collapsed after the reopen; COLLAPSE TILES makes every tile a bar with no ▾ on any, EXPAND TILES brings the full tiles back with the one you collapsed still a bar; the joined screens are one tile (A · Canvas A) and two again when dragged apart |
| 3 | The grid on the tiles | BUILD → Pattern: Grid with a 96 px cell and a 1 px line, EDIT SAFE on, look at the PGM and PVW panes and every wall tile in the switcher for a minute; then a 24 px cell with 4 subdivisions; then LED wall with tile borders and the pixel grid on; then a Grid on the multiview output's tiles; then SETUP → Screens | Every tile and pane shows every grid line, one pixel wide, steady while the show clock runs — no lines popping in and out; the 24 px cell reads as a fine grid whose minor lines are left out on the small tiles and present on the big panes; the LED wall's tile borders read on every tile; the multiview's tiles show the grid's lines; the Screens page's tiles show the grid; the outputs are pixel-exact as before (a 1 px line is one pixel on the wall) |
| 2 | The assistant answers | ADMIN → Assistant with a saved key: ask *What can you help me build?*; then *Two screens and a walk-in look with the clock*; APPLY a proposal; ask a third question | Every question is answered in words with proposals or questions, never *The assistant failed: … The compiled grammar is too large …*; if the status line carries *The service declined the reply's schema; the answer came as plain JSON and was read all the same* once, the answer still reads and the questions after it carry no such note; APPLY still lands the look with its overlays |
| 1 | No crash between pages | BUILD → Lower thirds: pick a design with a Fractal element (the *Fractal* preset, or add a Fractal element to any design) and let the designer preview play; with no output connected and then with one, switch BUILD → Pattern and back to Lower thirds as fast as the mouse allows, twenty times; do the same between SETUP → Screens and any other page; leave the desk running ten minutes with the fractal design playing in the preview | No exit, no *App crashed (exit -1073741819 …)* on the next start's health line; the Lower thirds preview draws again every time the page is re-entered (a fresh stage, the fractal moving); the Screens page's tiles draw again every time; the log carries no *Lower-third preview failed to draw* |
