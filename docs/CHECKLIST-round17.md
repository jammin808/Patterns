# Round 17 — Windows checklist

The headless suite proves the logic on every push. These are the things only a real rig shows:
one line per item, what to do, and what you should see. Tick them on a Windows machine with the
full build (libVLC bundled), two displays and an audio interface. The round came from a rig day —
a crash between pages, the switcher's tiles, a send that lost the preview, the grid on the tiles,
the assistant's first question — so the rows are the desk in use.

| # | Item | Do | Expect |
| --- | --- | --- | --- |
| 4 | The switcher's tiles | On the Build layout with three or more screens, maximise the window, then plug a display in or enable a screen so the tiles rebuild, and look at every tile; press ▸ at the end of a screen tile's title row, then ▾ on its bar; ▸ on PGM; save, close and reopen the show; SHOW → Run, ▸ COLLAPSE TILES then ◂ EXPAND TILES; on SETUP → Screens drag a screen flush against another, then apart | Every tile shows one face — the title row, the two miniatures, OWN / MON / ARM / LOCK — with the buttons at the top of the tile level with PGM's, and no name on its side over a body; ▸ makes that one tile a vertical bar (the tally, the name on its side, ▾) with the rest full, ▾ opens it again, PGM collapses the same way; the collapsed tiles come back collapsed after the reopen; COLLAPSE TILES makes every tile a bar with no ▾ on any, EXPAND TILES brings the full tiles back with the one you collapsed still a bar; the joined screens are one tile (A · Canvas A) and two again when dragged apart |
| 3 | The grid on the tiles | BUILD → Pattern: Grid with a 96 px cell and a 1 px line, EDIT SAFE on, look at the PGM and PVW panes and every wall tile in the switcher for a minute; then a 24 px cell with 4 subdivisions; then LED wall with tile borders and the pixel grid on; then a Grid on the multiview output's tiles; then SETUP → Screens | Every tile and pane shows every grid line, one pixel wide, steady while the show clock runs — no lines popping in and out; the 24 px cell reads as a fine grid whose minor lines are left out on the small tiles and present on the big panes; the LED wall's tile borders read on every tile; the multiview's tiles show the grid's lines; the Screens page's tiles show the grid; the outputs are pixel-exact as before (a 1 px line is one pixel on the wall) |
| 2 | The assistant answers | ADMIN → Assistant with a saved key: ask *What can you help me build?*; then *Two screens and a walk-in look with the clock*; APPLY a proposal; ask a third question | Every question is answered in words with proposals or questions, never *The assistant failed: … The compiled grammar is too large …*; if the status line carries *The service declined the reply's schema; the answer came as plain JSON and was read all the same* once, the answer still reads and the questions after it carry no such note; APPLY still lands the look with its overlays |
| 1 | No crash between pages | BUILD → Lower thirds: pick a design with a Fractal element (the *Fractal* preset, or add a Fractal element to any design) and let the designer preview play; with no output connected and then with one, switch BUILD → Pattern and back to Lower thirds as fast as the mouse allows, twenty times; do the same between SETUP → Screens and any other page; leave the desk running ten minutes with the fractal design playing in the preview | No exit, no *App crashed (exit -1073741819 …)* on the next start's health line; the Lower thirds preview draws again every time the page is re-entered (a fresh stage, the fractal moving); the Screens page's tiles draw again every time; the log carries no *Lower-third preview failed to draw* |
