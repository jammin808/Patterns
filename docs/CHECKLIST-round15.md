# Round 15 — Windows checklist

The headless suite proves the logic on every push. These are the things only a real rig shows:
one line per item, what to do, and what you should see. Tick them on a Windows machine with the
full build (libVLC bundled), two displays and an audio interface; row 5 also wants a phone on the
network and a Stream Deck with Companion at 2.6.0. Row 1 is the report's crash between menus: do
it first, on the machine it happened on, and after any restart read the health line before
touching anything — the sentence it shows is the evidence. Rows 5, 6 and 7 are the show-critical
ones (a fade, a take, the Run area). Rows 7 and 9 end in a judgment only a real monitor makes —
the popup's pause and the bands' strength — and a note of what you would change is the result.
The cloud question is answered in `docs/PLAN.md` §20.10; nothing on this list depends on it.

| # | Item | Do | Expect |
| --- | --- | --- | --- |
| 9 | The sections read apart | Open every page on the rail at the laptop's window size, then maximised; on BUILD → Pattern read the title and the section bands; on ADMIN → Machine read the tiles' edges; press each group on the rail and each page on the strip; PLAN → Show panel: read CUES, LOOKS, STINGERS and the rest | Each page's title sits in its own colour (the dot on its chip) a size bigger than before; every section heading is a bold band in that colour on a faint ground of the same colour, the sections plainly apart; the panels on a page are edged faintly in its colour; the current group on the rail and the current page on the strip carry a thin underline in their colour and nothing moves when the choice changes; the Show panel's groups read as bands in green; nothing on any page is clipped or pushed off at the laptop size |
| 8 | Layers on their own page | BUILD → read the rail; open Layers; with two displays, OWN on the second screen's tile and read EDITING TARGET on the Layers page; set LAYER 1 to a still and drag its box on the PREVIEW pane; set LAYER 2 to a web page and read the bottom of the page; open Media and read its bottom; ADMIN → Help: search *layers* and press the topic's page link; run the walkthrough *A web page and layers on a screen* to its Layers step | Layers sits between Branding and Library, before Library and Assistant; the page reads EDITING TARGET with the second screen picked and the banner *EDITING: SCREEN 2 …*, then LAYER 1 and LAYER 2 with the source, the box, the fit, the opacity, the corners, the border and the crop each had; the still appears over the picture and follows the drag; with a web layer PAGE CONTROLS appears under LAYER 2 and typing reaches the page; the Media page ends with its page controls and no LAYERS block; the help topic's page link opens the Layers page and the walkthrough's step goes there |
| 7 | The Run area's room | Two displays, a running order of twenty cues: SHOW → RUN; read where the divider between THE WALL and CUE STACK sits; drag it to the middle, EXIT RUN and RUN again, save the show, reopen it; press ▸ COLLAPSE TILES, click a bar, rest the pointer on a bar for a second, then move it away; press ◂ EXPAND TILES; rest the pointer on a full tile; POP OUT the Run surface and drag the divider there | The stack takes about a third of the width and the wall the rest; after the drag the stack holds the middle through EXIT RUN, RUN, a save and a reopen; collapsed, every tile is a narrow bar the wall's height with the tally on top and the name reading downwards (BLACK's badge as a B), a click on a bar still focuses that target, and a second's pause pops up the tile large with PGM and PVW side by side (moving away closes it); expanded, the full tiles come back and the pause over one pops it up the same way; the pop-out window shows the same divider and follows a drag made in either window |
| 6 | CUT / TAKE with a scope | Two displays, EDIT SAFE on: build a Grid in the preview; on the wall pick FOCUSED, click the second screen's tile, TAKE; build Colour bars, pick ALL ARMED, TAKE; tick both tiles, pick TICKED, build a Focus pattern, CUT; untick, CUT again; pick TICKED GROUPS with no canvas on the wall, TAKE; ARM off the first tile, tick both, TICKED, TAKE | The grid lands on the second screen alone with the first keeping its picture (its tile reads OWN, the status line *TAKE — sandbox faded up on 2 · Right (1 kept their picture)*); the bars land everywhere and the OWN on the first tile clears; the Focus pattern cuts (no fade) onto both ticked screens and the ticks clear; CUT with nothing ticked reads *Tick the wall tiles first.* and nothing moves; TICKED GROUPS with no canvas reads *Tick a group (a joined canvas) on the wall first.*; with the first tile ARM off, only the second takes and the line counts the first among those that kept their picture; EDIT SAFE re-arms after every send |
| 5 | Fade to black on one screen, and the sound | Two displays, a music track playing from the Audio page, a clip with sound on air; PLAN → Show panel: pick THE FOCUSED SCREEN, click the second screen's wall tile, FADE TO BLACK 2 s, then FADE UP; tick both tiles, pick THE TICKED SCREENS, FADE TO BLACK; FADE UP with EVERY SCREEN; pick EVERY SCREEN and FADE TO BLACK 3 s, then FADE UP; untick WITH THE SOUND and repeat; BLACKOUT on and off; from the phone's SCREENS tab press ▼ then ▲ on one screen; from Companion a FADE ▼ key on screen 1; a cue with *Fade to black* → *Screen 2* for 1 s, GO | The second screen alone fades to black over two seconds while the first keeps its picture and the music and the clip's sound keep playing; FADE UP brings it back the same way. Both ticked screens fade together and, as the last one goes dark, the music and the clip's sound fade down over the same two seconds; FADE UP on EVERY SCREEN brings the screens and the sound back. The rig fade over three seconds is a blackout with a fade, the sound going with it, and the fade up restores both; with WITH THE SOUND off the pictures fade and the sound stays. BLACKOUT on and off never move the sound. The phone's ▼ darkens that screen, its row reads BLACK, ▲ lifts it; the Companion key goes red while its screen is black; the cue darkens screen 2 for its second. The wall tile's tick is there with EDIT SAFE off. STATE (`/api/state`) reads `black.count`, `black.text` and `black.audio` |
| 4 | The Machine page's card lines and the tiles' bars | ADMIN → Machine on the laptop with OUTPUTS ON on two outputs and a fractal pattern up: read LIVE PERFORMANCE for a minute, then put a Grid up and read again; read HEALTH AT A GLANCE's CPU, MEMORY, GPU and DISK tiles; leave it an hour and read the day's lines; on a machine whose driver gives no GPU counters read the GPU row | Under Rendering, a GPU row (the card's busy share, 0–100) and a Video memory row (*in use 1.2 GB of 8.0 GB (15%)*) as lines like CPU and memory, three minutes beside the day, the busy line climbing under the fractal and settling under the Grid, the memory line steady (a line that only climbs is a leak); every tile's bar sits inside its pill with the pill's padding either side, none running past the edge; after an hour the day's lines for the card read the 30-second averages; a machine with no counters reads *busy n/a — no reading from this card* and a flat line, nothing missing from the page |
| 3 | The Show panel's stop and the wall's OUT | SETUP → Audio: a VOG and two stingers; BUILD → Lower thirds: a design and six people; PLAN → Show panel: fire a stinger and press ■ Stop; fire a held stinger and press PUT IT BACK; on the wall with two displays and the window at the laptop's size, press OUT on the second screen's tile, then again; open EDIT SAFE and read the tile's title row | ■ Stop and the STING HOLD banner sit right under the STINGERS chips (above LOWER THIRDS and the six people), the stop ends the stinger and the banner's PUT IT BACK the held one; every wall tile's title row ends with OUT — green while the screen's output is on, grey when it is off — inside the tile with nothing hanging past its edge, the bottom row reading OWN MON ARM LOCK and the size; OUT off closes that screen's output live and on brings it back; with EDIT SAFE open SEND and the tick sit beside OUT and the title trims rather than anything leaving the tile |
| 2 | The editing target never blank | Two displays: OWN on the second screen's tile, then on the Pattern page read EDITING TARGET; LOCK and unlock the first screen's tile; on the Screens page type a label for the second screen; OWN on the first tile too, then pick the second screen back in EDITING TARGET; OWN off on the second; OWN off on the first; load a saved show with own patterns; plug a display out and in | EDITING TARGET reads the second screen from the moment OWN goes on and keeps reading it through the lock, the unlock and the label (the label appears in the picker and in the EDITING banner at once) — never a blank box; the second OWN moves the editors to the first screen and picking the second back holds; OWN off puts the picker on Program (still shown while another screen has its own pattern), and with every own pattern gone the picker hides and the banner reads EDITING: PROGRAM; a loaded show and a display change never leave the box blank |
| 1 | The crash between menus, contained | Start Patterns through the watchdog; move through every page on the rail (SHOW, PLAN, BUILD, SETUP, ADMIN) and back, then RUN and EXIT RUN, at the laptop's window size and maximised, with OUTPUTS ON on two outputs and a clip playing; read STABILITY on the Machine page and the health line; then, to see the guard itself, type `HELP` on the console and press a key the page refuses — nothing should happen but the refusal; if the desk ever restarts, read the health line and STABILITY first, then `patterns.log` and the support bundle | Every switch is instant and the outputs never flinch; STABILITY reads *no faults*; if a page did throw, the status line reads *A fault was contained and the desk carried on — <type>: <message> — in <where>*, the health line counts it (*1 fault caught, show kept running (last hh:mm — UI fault contained …)*), `patterns.log` has the stack under *UI fault contained*, and the desk, the outputs, the clip and the remotes carry on; if the desk did restart, the health line and STABILITY read *The last run ended in an unhandled .NET exception (see patterns.log) — <type>: <message> — in <the app's frames> — at hh:mm:ss after n min* (a native exit code means §18.2 of the plan and the mini-dump instead) — send that sentence with the bundle |
