# Round 14 — Windows checklist

The headless suite proves the logic on every push. These are the things only a real rig shows:
one line per item, what to do, and what you should see. Tick them on a Windows machine with the
full build (libVLC bundled), two displays, an audio interface and, where named, a phone on the
network and a Stream Deck with Companion. The low-spec laptop the report came from is the right
machine for rows 1 and 2.

| # | Item | Do | Expect |
| --- | --- | --- | --- |
| 8 | The phone remote's OVERLAYS tab | On the phone: OVERLAYS; CLOCK, then 12 H, SECONDS, DATE; type words into the message box, SHOW, SCROLL, HIDE; 5 MIN, then type 2:30 and START, then a time ten minutes ahead and COUNT DOWN TO THAT TIME, a label, STOP; LOGO, PIP, WEATHER; ALL OVERLAYS OFF; with EDIT SAFE open on the desk press CLOCK again; from a Stream Deck or a TCP line send `MESSAGE Doors open at 7`, `COUNTDOWN 1`, `OVERLAYS OFF` | Every press changes the screens at once and the key lights from the air (the clock key reads CLOCK — ON, its hours key reads the format the screens show); the message box shows the words on screen and stops following while you type; the countdown line counts every second in large type, reads OVER in red when it ends, and STOP clears it; the logo needs a file on the Branding page (the reply says so when none is set); with EDIT SAFE open the clock changes on the outputs while the desk's edited copy stays as it was; the wire lines do the same as the phone and the phone's keys follow them within a second |
| 7 | The Show panel's chips three to a row, lit while live | SETUP → Audio: three VOGs and four stingers (one an effect pulse, one with *hold for your take*); BUILD → Lower thirds: two designs and five people; PLAN → Show panel: fire a VOG, then a stinger, then the held stinger; press a person; open EDIT SAFE, PVW FIRST on, press the next person, TAKE TO AIR; ■ Hide; on the Lower thirds page type a different name into the design on air | VOG, STINGERS, LOWER THIRDS and PEOPLE sit three to a row, every chip the name on top and a small line under it (the kind, the after-choice, the person the design holds, the role); a playing VOG's chip goes red with *ON AIR · n s* on the line and back when it ends, a sting's line counts down what is left, the held stinger reads HOLDING until PUT IT BACK; the person pressed lights red with *ON AIR · <design>* and so does the design with *ON AIR* (ARRIVING while it comes in, LEAVING as it goes); the next person lights green with *IN PREVIEW · <design>* beside the first still red; TAKE swaps them; ■ Hide darkens every chip and the lines read the roles again; the typed-over name darkens the person's chip while the design stays red |
| 6 | The Lower thirds page: the preview pinned, a person folded | BUILD → Lower thirds on the laptop's screen: pick a design, scroll to ELEMENTS and change an element's shadow, glow and position, then its keys; put the design on air from the Show panel and PVW another; scroll to LIBRARY, + PERSON, type a name and a role, open More, add a company and a photo, close it; select another person; make the window narrower and shorter | The stage and its ▶ PLAY / slider stay at the top while the rest of the page scrolls under them, and every edit shows on the stage without scrolling back; the PREVIEW line names the design and its element count and reads *· ON AIR* or *· IN PREVIEW* while it is up; a person's row in the list reads the name and the role only; the editor shows Name and Role with a *More — Acme Ltd · photo · no note* line that reads the entry without opening it; More stays open or closed across the selection change; a small window scrolls the page, never the preview or the desk's tabs |
| 5 | The quality ladder and the memory ceilings | On the laptop: OUTPUTS ON on two outputs with a Particles pattern at 8000 particles and glow, or a Fractal at Fine quality with 400 iterations; watch the STABILITY render line and Machine → QUALITY LADDER for a minute; then PLAN → Pattern: a Grid; wait forty seconds; set QUALITY LADDER to Full and put the heavy pattern back; then Economy; then Auto; read MEMORY CEILINGS after an hour with pictures, decks and clips cycled; run SUPER-CHECK | With the heavy pattern the render line reads a worst frame past 25 ms for three seconds, then the QUALITY LADDER line reads *Auto, level 1 of 3: particles and fractal iterations at 70% (Output 1 (Main): frames past 25 ms for 3 s)* and both outputs and the preview thin out together — the same particles on both, no jump or re-seed; if still slow, level 2 within another three seconds; with the Grid up the line reads *Auto: full quality … Stepped down n times this session and came back* after thirty clean seconds; Full holds full quality with the heavy pattern and the render line stays red; Economy reads *locked at level 2 of 3* at once; Auto resumes from level 2 and climbs back; MEMORY CEILINGS reads *This app N MB of a M GB ceiling (X GB machine) · pictures n of 10 cached · decoders n of 4 · n frames held for fades* with the pictures never past 10 and the decoders never past 4; the super-check's *Quality ladder* and *Memory ceiling* rows say the same |
| 4 | The standby cue's clip pre-rolled | PLAN → Cues: a cue whose look is a video clip (a VT from the stick) and a plain cue after it; ARM; put the VT cue on standby and watch the Run strip and SETUP → Media's active inputs; wait five seconds; GO; move standby back to the VT cue while it plays; put four other clips up (a pattern clip, two layers and a PiP) and standby the VT cue again; restart Patterns with the stick pulled and standby the VT cue | Within a second of standby the strip shows PRE-ROLLED (green) beside STANDBY and the Media page lists the clip as *pre-rolled*; nothing is heard and the outputs do not change; GO cuts straight onto the clip's first frame — no black, no held frame of the previous picture, the sound starting with the picture; standby back on the playing clip reads CLIP ON AIR and the clip is not disturbed; with four decoders live the strip reads CLIP NOT OPEN and GO still plays the clip (it opens at the press); with the stick pulled the strip reads CLIP NOT OPEN and `patterns.log` has the open failure |
| 3 | The engine's frame budget and the start-up budget | Start Patterns through the watchdog and read STABILITY on the Machine page as soon as the desk is up; OUTPUTS ON on a look with two outputs; put up a lower third with a fractal element and, on the Pattern page, a Fractal pattern at Fine quality with high iterations; watch the render line for a minute, then RUN SUPER-CHECK and read the *Render frame* and *Start-up* rows and the RENDER tile; lower the fractal's iterations, wait a minute, read again; unplug a USB drive the settings' media paths point at and restart | The start-up line reads *Start-up N s: runtime … · settings … · services … · window … · first frame …* within seconds of the desk appearing, and the super-check's *Start-up* row is green under 8 s; the render line names the worst frame of the last minute with its stage and sink — *Render frame worst 31.2 ms (the lower third) on Output 1 (Main) in the last minute · Preview … · Output 1 (Main) … at 60 fps · n past 25 ms this session* — and the *Render frame* row goes amber past 25 ms with advice naming the fractal or the lower third; the RENDER tile shows the same number with a bar; after lowering the iterations the row is back to green within a minute and the slow count stops growing; a restart with a missing media drive shows the longest phase as *services* and the *Start-up* row amber or red with that phase named |
| 2 | Native-crash hardening — the note, the dump, the safe run | On the laptop the report came from, with the published build: check `createdump.exe` sits beside `Patterns.exe`; start Patterns through the watchdog and read the first lines of `patterns.watchdog.log`; on Machine → VIDEO DECODING read the line; run the show as on the night (video stingers, a lower third with a fractal element on two outputs, the pulse) and if it faults again let the watchdog bring it back, then read the health line, STABILITY on the Machine page, `patterns.log`, the `crashes` folder and a support bundle; then set Software and run the same show; with a fractal lower third up on two outputs watch CPU in Task Manager | The watchdog log's first lines say *Mini-dumps on a native crash: on, into …\crashes (0 kept)*; VIDEO DECODING reads *Auto: clips decode on the graphics card*; after a fault the health line and STABILITY read *The last run ended in an access violation (…) at hh:mm:ss after … min; a mini-dump is at …\crashes\patterns-<pid>-<time>.dmp. Video decoding is in software for this run*, VIDEO DECODING reads *Auto, in a safe run: …*, the crashes folder holds that dump, `bundle-info.txt` in the support bundle names it and the zip holds it under 60 MB; a show that then runs clean in software points at the graphics driver's decoder — leave Software set on that machine; two native faults in a row and the health line says the decoder is not the cause and asks for the bundle; with the fractal lower third up the CPU sits below the previous build's and the fractal still moves smoothly, and no clip stalls while it is up |
| 1 | The stinger and VOG lifecycle — a clip that cannot be stopped, no more | SETUP → Audio: two video stingers (A and B) and a VOG sound; OUTPUTS ON on a look; fire A from the Show panel and let it end; fire A again and, while it plays, fire A a third time from the phone; fire A and press ■ Stop from the Audio page mid-clip; fire A, and while it plays pull the USB stick the clip is on (or rename the file) and wait twenty seconds; with EDIT SAFE off fire A, open EDIT SAFE mid-clip, let it end, close EDIT SAFE, fire A again; fire A, and while it plays fire the VOG sound and then B; type `STINGER STOP` in the console with nothing playing; open `patterns.log` and the journal | A ends and the look comes back every time; the third press restarts A from the top on the same decoder (the picture jumps to its start, no black); ■ Stop mid-clip puts the look back at once and the status reads *Ready.*; the pulled clip reads *Clip stalled — previous content back* (or *could not play*) within twenty seconds and the look is back, with a *Failed* line in the journal; after the EDIT SAFE dance the second fire of A plays from the top rather than fading straight back; the VOG ducks A and B replaces it, and after B the look is back, never a dead A; `STINGER STOP` with nothing up answers *Ready.*; the log has no *Stinger tick failed* line, and if it ever has one the clip still ends and the look still comes back |
