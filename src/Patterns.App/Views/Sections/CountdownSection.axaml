<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Patterns.App.ViewModels"
             xmlns:c="using:Patterns.App.Views.Controls"
             x:Class="Patterns.App.Views.Sections.CountdownSection"
             x:DataType="vm:MainViewModel">

  <StackPanel Classes="form" Margin="4,8,12,16">
    <TextBlock Classes="h1 neon-time" Text="Countdown" />

    <CheckBox Content="Countdown on" IsChecked="{Binding State.Countdown.Enabled}" />
    <TextBlock Classes="hint" Text="{Binding CountdownPreview}" />

    <TextBlock Classes="h2" Text="LABEL" />
    <TextBox Text="{Binding State.Countdown.Label}" Watermark="SHOW STARTS IN" />
    <ItemsControl ItemsSource="{Binding CountdownLabels}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate>
        <DataTemplate x:DataType="x:String">
          <Button Classes="chip" Margin="0,2,6,2" Content="{Binding}"
                  Command="{Binding $parent[UserControl].((vm:MainViewModel)DataContext).ApplyCountdownLabelCommand}"
                  CommandParameter="{Binding}" />
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>

    <TextBlock Classes="h2" Text="TARGET" />
    <ComboBox ItemsSource="{Binding CountdownKinds}"
              SelectedValue="{Binding State.Countdown.TargetKind, Mode=TwoWay}"
              SelectedValueBinding="{Binding Value, x:DataType=vm:EnumItem}" />
    <Grid ColumnDefinitions="130,*">
      <TextBlock Classes="label" Text="Time (HH:mm)" />
      <TextBox Grid.Column="1" Text="{Binding State.Countdown.TargetTime}" Watermark="19:30" />
    </Grid>
    <Grid ColumnDefinitions="130,*,8,Auto">
      <TextBlock Classes="label" Text="Duration (min)" />
      <NumericUpDown Grid.Column="1" Minimum="0.1" Maximum="1440" Increment="1" FormatString="0.#"
                     Value="{Binding State.Countdown.DurationMinutes, Mode=TwoWay, Converter={StaticResource Num}}" />
      <Button Grid.Column="3" Classes="chip" Content="Start now" Command="{Binding ArmCountdownCommand}"
              ToolTip.Tip="Arms the duration countdown from this moment" />
    </Grid>

    <TextBlock Classes="h2" Text="AT ZERO" />
    <ComboBox ItemsSource="{Binding CountdownEnds}"
              SelectedValue="{Binding State.Countdown.EndBehavior, Mode=TwoWay}"
              SelectedValueBinding="{Binding Value, x:DataType=vm:EnumItem}" />
    <TextBox Text="{Binding State.Countdown.EndMessage}" Watermark="STARTING NOW" />

    <TextBlock Classes="h2" Text="LOOK" />
    <Grid ColumnDefinitions="130,*">
      <TextBlock Classes="label" Text="Position" />
      <ComboBox Grid.Column="1" ItemsSource="{Binding Anchors}"
                SelectedValue="{Binding State.Countdown.Anchor, Mode=TwoWay}"
                SelectedValueBinding="{Binding Value, x:DataType=vm:EnumItem}" />
    </Grid>
    <Grid ColumnDefinitions="130,*">
      <TextBlock Classes="label" Text="Size (% height)" />
      <Slider Grid.Column="1" Minimum="4" Maximum="45" Value="{Binding State.Countdown.SizePct, Mode=TwoWay}" />
    </Grid>
    <CheckBox Content="Progress bar" IsChecked="{Binding State.Countdown.ShowProgressBar}" />
    <Grid ColumnDefinitions="130,*">
      <TextBlock Classes="label" Text="Digits colour" />
      <c:ColorField Grid.Column="1" Value="{Binding State.Countdown.TextColor, Mode=TwoWay}" />
    </Grid>

    <TextBlock Classes="hint tip" Margin="0,8,0,0"
               Text="The countdown overlays whatever pattern is on screen — put it over branding colours, particles or a flat field for classic walk-in looks. Times within the last 12 hours read as already reached; later ones roll to tomorrow (so 00:30 armed at 23:00 counts 1.5 h)." />
  </StackPanel>
</UserControl>
