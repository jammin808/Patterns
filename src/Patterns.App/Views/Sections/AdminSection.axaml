<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Patterns.App.ViewModels"
             x:Class="Patterns.App.Views.Sections.AdminSection"
             Classes="hue-machine"
             x:DataType="vm:MainViewModel">

  <UserControl.Styles>
    <Style Selector="Border.hTile">
      <Setter Property="Width" Value="156" />
      <Setter Property="MinHeight" Value="92" />
      <Setter Property="Background" Value="{StaticResource PanelBg2Brush}" />
      <Setter Property="CornerRadius" Value="10" />
      <Setter Property="Padding" Value="11,8,11,9" />
      <Setter Property="Margin" Value="0,0,8,8" />
      <Setter Property="BorderThickness" Value="0,0,0,3" />
    </Style>
    <Style Selector="TextBlock.hTitle">
      <Setter Property="FontSize" Value="11" />
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="LetterSpacing" Value="1.5" />
      <Setter Property="Foreground" Value="{StaticResource TextDimBrush}" />
    </Style>
    <Style Selector="TextBlock.hValue">
      <Setter Property="FontSize" Value="24" />
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="Margin" Value="0,2,0,0" />
    </Style>
    <Style Selector="Border.spark">
      <Setter Property="Width" Value="304" />
      <Setter Property="Height" Value="60" />
      <Setter Property="BorderBrush" Value="#33FFFFFF" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="Background" Value="{StaticResource PanelBg2Brush}" />
    </Style>
  </UserControl.Styles>

  <StackPanel Classes="form" Margin="4,8,12,16">
    <TextBlock Classes="h1 neon-admin" Text="Machine" />
    <TextBlock Classes="hint tip"
               Text="The machine room, read at a glance: one headline over twelve lit tiles (outputs, render, CPU, memory, GPU, NDI, stream, audio, remote, watchdog, power, disk), the warnings and what to do about them, the last three minutes and the day so far as lines, then the one-button super-check, the graphics card choice, and the tools for keeping a show machine healthy." />

    <!-- Round 73: the machine's settings — show lock, rig day games, the watchdog, the beacon, the twin, the earlier versions —
         live in the pop-out column beside this page, so the page is the health and performance lines. -->
    <Grid ColumnDefinitions="*,8,Auto" Margin="0,4,0,0">
      <TextBlock Classes="hint" TextWrapping="Wrap" VerticalAlignment="Center"
                 Text="Show lock, rig day games, the watchdog, the beacon, the twin and the earlier versions are in the column beside this page. ◀ CLOSE hides the column; SETTINGS ▸ brings it back." />
      <Button Grid.Column="2" Classes="mini" Content="SETTINGS ▸" Command="{Binding OpenPopOutCommand}" IsVisible="{Binding !PopOut.IsOpen}"
              ToolTip.Tip="Open the machine's settings in the column beside this page" />
    </Grid>

    <TextBlock Classes="h2" Text="HEALTH AT A GLANCE" />
    <Border Classes="runRow">
      <Grid ColumnDefinitions="Auto,12,*,12,Auto">
        <Ellipse Width="18" Height="18" Fill="{Binding DashboardDot}" VerticalAlignment="Center" />
        <StackPanel Grid.Column="2" Spacing="2">
          <TextBlock Text="{Binding DashboardHeadline}" FontSize="17" FontWeight="Bold" TextWrapping="Wrap" />
          <TextBlock Classes="hint" Text="{Binding DashboardDetail}" TextWrapping="Wrap" />
        </StackPanel>
        <TextBlock Grid.Column="4" Classes="runTag" Text="{Binding DashboardUptime}" VerticalAlignment="Center" />
      </Grid>
    </Border>
    <ItemsControl ItemsSource="{Binding DashboardTiles}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <WrapPanel />
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate>
        <DataTemplate x:DataType="vm:DashboardTileView">
          <!-- One tile: the light on the bottom edge and beside the title, the big value, a line, a bar when the value is a share. -->
          <Border Classes="hTile" BorderBrush="{Binding Dot}" ClipToBounds="True">
            <StackPanel Spacing="2">
              <Grid ColumnDefinitions="*,Auto">
                <TextBlock Classes="hTitle" Text="{Binding Title}" VerticalAlignment="Center" />
                <Ellipse Grid.Column="1" Width="9" Height="9" Fill="{Binding Dot}" VerticalAlignment="Center" />
              </Grid>
              <TextBlock Classes="hValue" Text="{Binding Value}" TextTrimming="CharacterEllipsis" />
              <TextBlock Classes="hint" Text="{Binding Detail}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding Detail}" />
              <!-- The bar takes the tile's width and no more: the theme's own minimum width is wider than the pill (round 15) -->
              <ProgressBar IsVisible="{Binding HasBar}" Minimum="0" Maximum="1" Value="{Binding Fraction}" Height="4" MinWidth="0" MinHeight="0"
                           HorizontalAlignment="Stretch" Foreground="{Binding Dot}" Margin="0,3,0,0" />
            </StackPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <TextBlock Classes="hint tip"
               Text="Green is fine, amber wants a look, red needs fixing now, grey is not in use or not known. The values are live (every second); the rig's facts — sends, the stream, the audio devices, the remote, the watchdog — are read every five seconds. The headline is the worst of the tiles and the advice below, naming the tiles that set it; the same thresholds as the super-check." />

    <TextBlock Classes="h2" Text="WARNINGS AND RECOMMENDATIONS" />
    <ItemsControl ItemsSource="{Binding AdminSuggestions}">
      <ItemsControl.ItemTemplate>
        <DataTemplate x:DataType="vm:SuggestionRow">
          <Border Classes="runRow" BorderBrush="{Binding Dot}" BorderThickness="4,1,1,1">
            <StackPanel Spacing="2">
              <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" />
              <TextBlock Classes="hint" Text="{Binding Detail}" TextWrapping="Wrap" />
            </StackPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <TextBlock Classes="hint tip"
               Text="Each card is what is wrong, why it matters mid-show and the next thing to do — red for a warning, amber for advice, grey for a note. The list is ordered worst first and changes as the numbers move; a leak, a frozen render path, a stream that stopped by itself and a machine on battery all appear here before they appear on a screen." />

    <TextBlock Classes="h2" Text="SUPER-CHECK" />
    <TextBlock Classes="hint tip" TextWrapping="Wrap"
               Text="One button: the machine, the graphics card, every display and its mode, the outputs and their frame rate, NDI, the stream, audio, the remote, video playback and the advice — each with a light — and the level of show this hardware is good for. The report is saved beside the exe and can be copied into an email." />
    <WrapPanel>
      <Button Classes="go" Content="RUN SUPER-CHECK" Padding="14,8" Margin="0,0,8,6" Command="{Binding RunSuperCheckCommand}"
              ToolTip.Tip="Checks everything now and writes patterns.supercheck.txt beside the exe" />
      <Button Classes="chip" Content="Copy report" Click="CopySuperCheck" Margin="0,0,8,6" IsVisible="{Binding HasSuperCheck}"
              ToolTip.Tip="The whole report as text — paste it into an email or ticket" />
    </WrapPanel>
    <Border Classes="panel" Padding="12" IsVisible="{Binding HasSuperCheck}">
      <StackPanel Spacing="6">
        <StackPanel Orientation="Horizontal" Spacing="8">
          <Ellipse Width="12" Height="12" Fill="{Binding SuperCheckDot}" VerticalAlignment="Center" />
          <TextBlock Text="{Binding SuperCheckHeadline}" FontWeight="Bold" TextWrapping="Wrap" VerticalAlignment="Center" />
        </StackPanel>
        <TextBlock Classes="label" Text="{Binding SuperCheckLevelText}" TextWrapping="Wrap" />
        <TextBlock Classes="hint" Text="{Binding SuperCheckSavedText}" TextWrapping="Wrap" />
        <ItemsControl ItemsSource="{Binding SuperCheckRows}">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="vm:CheckRowView">
              <Border Width="330" Background="{StaticResource PanelBg2Brush}" CornerRadius="6" Padding="8,6" Margin="0,0,6,6">
                <Grid ColumnDefinitions="Auto,8,*">
                  <Ellipse Width="9" Height="9" Fill="{Binding Dot}" VerticalAlignment="Top" Margin="0,4,0,0" />
                  <StackPanel Grid.Column="2">
                    <TextBlock Classes="hint" Text="{Binding Section}" />
                    <TextBlock FontWeight="SemiBold" TextWrapping="Wrap">
                      <Run Text="{Binding Item}" /><Run Text=" — " /><Run Text="{Binding Value}" />
                    </TextBlock>
                    <TextBlock Classes="hint" Text="{Binding Note}" TextWrapping="Wrap" IsVisible="{Binding HasNote}" />
                  </StackPanel>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </StackPanel>
    </Border>

    <TextBlock Classes="h2" Text="LIVE PERFORMANCE — THE LAST 3 MINUTES, AND THE DAY SO FAR" />
    <Grid ColumnDefinitions="86,Auto,12,Auto" Margin="0,2">
      <TextBlock Classes="label" Text="CPU" VerticalAlignment="Top" Margin="0,2,0,0" />
      <StackPanel Grid.Column="1" Spacing="3">
        <TextBlock Classes="hint" Text="{Binding AdminCpuText}" Width="304" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding AdminCpuText}" />
        <Border Classes="spark">
          <Polyline Points="{Binding AdminCpuSpark}" Stroke="#3EC1F3" StrokeThickness="1.5" Margin="2" />
        </Border>
      </StackPanel>
      <StackPanel Grid.Column="3" Spacing="3">
        <TextBlock Classes="hint" Text="today · whole computer" />
        <Border Classes="spark">
          <Polyline Points="{Binding AdminCpuDaySpark}" Stroke="#3EC1F3" StrokeThickness="1.5" Margin="2" />
        </Border>
      </StackPanel>
    </Grid>
    <Grid ColumnDefinitions="86,Auto,12,Auto" Margin="0,4">
      <TextBlock Classes="label" Text="Memory" VerticalAlignment="Top" Margin="0,2,0,0" />
      <StackPanel Grid.Column="1" Spacing="3">
        <TextBlock Classes="hint" Text="{Binding AdminMemText}" Width="304" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding AdminMemText}" />
        <Border Classes="spark">
          <Polyline Points="{Binding AdminRamSpark}" Stroke="#B18CFF" StrokeThickness="1.5" Margin="2" />
        </Border>
      </StackPanel>
      <StackPanel Grid.Column="3" Spacing="3">
        <TextBlock Classes="hint" Text="today · this app — a line that only climbs is a leak" />
        <Border Classes="spark">
          <Polyline Points="{Binding AdminRamDaySpark}" Stroke="#B18CFF" StrokeThickness="1.5" Margin="2" />
        </Border>
      </StackPanel>
    </Grid>
    <Grid ColumnDefinitions="86,Auto,12,Auto" Margin="0,4">
      <TextBlock Classes="label" Text="Rendering" VerticalAlignment="Top" Margin="0,2,0,0" />
      <StackPanel Grid.Column="1" Spacing="3">
        <TextBlock Classes="hint" Text="{Binding AdminRenderText}" Width="304" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding AdminRenderText}" />
        <Border Classes="spark">
          <Polyline Points="{Binding AdminFpsSpark}" Stroke="#2EE68A" StrokeThickness="1.5" Margin="2" />
        </Border>
      </StackPanel>
      <StackPanel Grid.Column="3" Spacing="3">
        <TextBlock Classes="hint" Text="today · frames per second" />
        <Border Classes="spark">
          <Polyline Points="{Binding AdminFpsDaySpark}" Stroke="#2EE68A" StrokeThickness="1.5" Margin="2" />
        </Border>
      </StackPanel>
    </Grid>
    <!-- The card as lines like CPU and memory (round 15): its busy share, and its memory in use against its total -->
    <Grid ColumnDefinitions="86,Auto,12,Auto" Margin="0,4">
      <TextBlock Classes="label" Text="GPU" VerticalAlignment="Top" Margin="0,2,0,0" />
      <StackPanel Grid.Column="1" Spacing="3">
        <TextBlock Classes="hint" Text="{Binding AdminGpuText}" Width="304" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding AdminGpuText}" />
        <Border Classes="spark">
          <Polyline Name="GpuSpark" Points="{Binding AdminGpuSpark}" Stroke="#FF9E5B" StrokeThickness="1.5" Margin="2" />
        </Border>
      </StackPanel>
      <StackPanel Grid.Column="3" Spacing="3">
        <TextBlock Classes="hint" Text="today · the card's busy share" />
        <Border Classes="spark">
          <Polyline Points="{Binding AdminGpuDaySpark}" Stroke="#FF9E5B" StrokeThickness="1.5" Margin="2" />
        </Border>
      </StackPanel>
    </Grid>
    <Grid ColumnDefinitions="86,Auto,12,Auto" Margin="0,4">
      <TextBlock Classes="label" Text="Video memory" VerticalAlignment="Top" Margin="0,2,0,0" TextWrapping="Wrap" />
      <StackPanel Grid.Column="1" Spacing="3">
        <TextBlock Classes="hint" Text="{Binding AdminVramText}" Width="304" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding AdminVramText}" />
        <Border Classes="spark">
          <Polyline Name="VramSpark" Points="{Binding AdminVramSpark}" Stroke="#F36FB1" StrokeThickness="1.5" Margin="2" />
        </Border>
      </StackPanel>
      <StackPanel Grid.Column="3" Spacing="3">
        <TextBlock Classes="hint" Text="today · the card's memory in use, against its total — a line that only climbs is a leak" />
        <Border Classes="spark">
          <Polyline Points="{Binding AdminVramDaySpark}" Stroke="#F36FB1" StrokeThickness="1.5" Margin="2" />
        </Border>
      </StackPanel>
    </Grid>
    <TextBlock Classes="hint" Text="{Binding AdminExtrasText}" />
    <TextBlock Classes="hint" Text="{Binding AdminDayText}" />
    <TextBlock Classes="hint tip"
               Text="A 30-second summary is appended to patterns.metrics.csv beside the exe (rotated at 1 MB) — after a bad night, the record shows what CPU, memory and frame rate were doing." />
    <CheckBox Content="Keep the on-disk performance record (patterns.metrics.csv)"
              IsChecked="{Binding State.Admin.MetricsCsv}" />

    <TextBlock Classes="h2" Text="GRAPHICS CARD" />
    <ItemsControl ItemsSource="{Binding GpuRows}">
      <ItemsControl.ItemTemplate>
        <DataTemplate x:DataType="vm:GpuRow">
          <StackPanel Margin="0,2">
            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="Wrap" />
            <TextBlock Classes="hint" Text="{Binding Detail}" TextWrapping="Wrap" />
          </StackPanel>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <Grid ColumnDefinitions="60,*" Margin="0,4,0,0">
      <TextBlock Classes="label" Text="Use" VerticalAlignment="Center" />
      <ComboBox Grid.Column="1" MinWidth="240" HorizontalAlignment="Stretch"
                ItemsSource="{x:Static vm:Lists.GpuPreferences}"
                SelectedValue="{Binding State.Admin.Graphics.Preference, Mode=TwoWay}"
                SelectedValueBinding="{Binding Value, x:DataType=vm:EnumItem}" />
    </Grid>
    <Grid ColumnDefinitions="60,*" Margin="0,4,0,0" IsVisible="{Binding GpuSpecificVisible}">
      <TextBlock Classes="label" Text="Adapter" VerticalAlignment="Center" />
      <ComboBox Grid.Column="1" HorizontalAlignment="Stretch" ItemsSource="{Binding GpuAdapterNames}"
                SelectedItem="{Binding State.Admin.Graphics.AdapterName, Mode=TwoWay}" />
    </Grid>
    <Grid ColumnDefinitions="60,*" Margin="0,4,0,0">
      <TextBlock Classes="label" Text="Direct" VerticalAlignment="Top" Margin="0,2,0,0"
                 ToolTip.Tip="Direct output: outputs that bypass the desktop compositor (Screens page, the selected screen)" />
      <TextBlock Grid.Column="1" Classes="hint" Text="{Binding DirectOutputSummary}" TextWrapping="Wrap" />
    </Grid>
    <TextBlock Classes="label" Text="{Binding GpuActiveText}" />
    <TextBlock Classes="label" Text="{Binding GraphicsApplyStatus}" TextWrapping="Wrap" />
    <TextBlock Classes="hint tip"
               Text="By default Patterns detects the best card (most video memory, discrete first) and registers it with Windows so video decoding uses the same card — on laptops this is what stops the show rendering on the battery-saver GPU. Changing it needs an app restart." />

    <TextBlock Classes="h2" Text="VIDEO DECODING" />
    <Grid ColumnDefinitions="60,*" Margin="0,4,0,0">
      <TextBlock Classes="label" Text="Clips" VerticalAlignment="Center" />
      <ComboBox Grid.Column="1" MinWidth="240" HorizontalAlignment="Stretch"
                ItemsSource="{x:Static vm:Lists.VideoDecodings}"
                SelectedValue="{Binding State.Admin.VideoDecoding, Mode=TwoWay}"
                SelectedValueBinding="{Binding Value, x:DataType=vm:EnumItem}"
                ToolTip.Tip="Where video clips decode. Auto uses the graphics card, except in the run right after a native crash (an access violation), which decodes in software to rule the driver out" />
    </Grid>
    <TextBlock Classes="label" Text="{Binding VideoDecodingText}" TextWrapping="Wrap" />
    <TextBlock Classes="hint tip"
               Text="A crash with exit code 0xC0000005 is a native fault — a decoder, a driver or a library, not Patterns' own code. The watchdog notes it here and on the health line, keeps a mini-dump of the next one in the crashes folder (when createdump.exe sits beside the exe), and the run after it decodes clips in software. A laptop whose graphics driver keeps faulting is steadier on Software." />

    <TextBlock Classes="h2" Text="QUALITY LADDER" />
    <Grid ColumnDefinitions="60,*" Margin="0,4,0,0">
      <TextBlock Classes="label" Text="Effects" VerticalAlignment="Center" />
      <ComboBox Grid.Column="1" MinWidth="240" HorizontalAlignment="Stretch"
                ItemsSource="{x:Static vm:Lists.QualityModes}"
                SelectedValue="{Binding State.Admin.Quality, Mode=TwoWay}"
                SelectedValueBinding="{Binding Value, x:DataType=vm:EnumItem}"
                ToolTip.Tip="How the particles and fractals answer slow frames. Auto steps them down when an output's seconds press past its own rate's budget for three seconds — 85 % of the frame slot on a second's p95 (14 ms at 60 fps, 28 at 30), three slots missed, or a stutter — and back up after thirty clean seconds, and starts where the last session on this machine settled; Full never steps; Balanced and Economy lock a level" />
    </Grid>
    <TextBlock Classes="label" Text="{Binding QualityText}" TextWrapping="Wrap" />
    <TextBlock Classes="hint tip"
               Text="Auto is a game engine's dynamic quality, for the effects: when an output's seconds press past its own rate's budget for three seconds — 85 % of the frame slot on the second's p95 (14 ms at 60 fps, 28 at 30), three presentation slots missed, or a frame past 50 ms — particles and fractal iterations step down a level (70 %, then 50 %, then 35 %) on every sink at once — the outputs, the NDI feed and the preview keep the same picture — and step back up after thirty clean seconds. Auto starts where the last session on this machine settled (a machine under 8 GB starts a level down until thirty clean seconds prove it); a test card, text and a lower third's words are never scaled. Full never steps down (a slow frame stays slow and the frame budget says so); Balanced and Economy lock a level for a small laptop from the first minute." />

    <TextBlock Classes="h2" Text="MEMORY CEILINGS" />
    <TextBlock Classes="label" Text="{Binding MemoryBudgetText}" TextWrapping="Wrap" />
    <TextBlock Classes="hint tip"
               Text="The app's memory is bounded by design, and this line says where it is. The ceiling is a quarter of the machine (never under 512 MB, never over 3 GB): past it the super-check's Memory ceiling row goes amber, and the memory line above says whether the working set climbed steadily (a leak) or jumped (a very large picture or deck). The pictures cached are bounded in bytes by the machine's class (128 MB under 8 GB, 256 to 32 GB, 512 past it) and least recently drawn go first; every live source decodes into a pool of four to eight frames sized once (48, 64 or 96 MB a source), so a steady second of video allocates nothing and a source's memory is fixed whatever it plays; the frames a fade still needs are held 400 ms and no more; Skia's GPU cache is sized to the machine too. After the ceiling words, placed: the owners in megabytes — pictures, frame pools, frames held, deck pages, the managed heap — so a climb the ledger cannot place is a leak in what it does not see" />

    <TextBlock Classes="h2" Text="THIS COMPUTER" />
    <TextBlock Classes="label" Text="{Binding MachineOverview}" TextWrapping="Wrap" LineHeight="20" />
    <WrapPanel Margin="0,6,0,0">
      <Button Classes="chip" Content="Copy support info" Click="CopySupportInfo" Margin="0,0,8,6"
              ToolTip.Tip="Machine, GPUs, screens, current numbers and advice — paste into an email or ticket" />
      <Button Classes="chip" Content="Open app folder" Command="{Binding OpenAppFolderCommand}" Margin="0,0,8,6"
              ToolTip.Tip="Settings, logs, presets and the metrics record live beside Patterns.exe" />
    </WrapPanel>
    <TextBlock Classes="hint tip"
               Text="Everything Windows will say about this machine's graphics, audio and processing, read every half minute on a worker (never on the desk's thread): the Windows edition and build, the CPU and the memory, every graphics adapter with the driver that runs it (NVIDIA's own number derived), every display with its mode, its connector, its encoding and the EDID it presents, every audio endpoint with its shared-mode format, the power plan, hardware GPU scheduling, Game DVR and the overlay planes. The same words are in the support bundle, in STATE (machine.inventory), in RIG STATUS on the wire and in the assistant's facts." />

    <TextBlock Classes="h2" Text="COMMISSIONING" />
    <TextBlock Classes="label" Text="{Binding CommissioningText}" TextWrapping="Wrap" LineHeight="20" FontFamily="Consolas,monospace" FontSize="12" />
    <TextBlock Classes="hint tip"
               Text="The rig commissioned in seven stages, each judged from the desk's own evidence and each saying what to do next: DISCOVER (Windows shows every display, none lost), ASSIGN (every planned screen adopted), CONTRACT (every screen says what its link carries — a screen on TEST ROUTE is not contracted yet), CAPABILITY (every display's EDID offers the contract), OUTPUT TEST (the outputs have opened — IDENTIFY and a test card on the wall), VERIFY (Windows reports MATCH on every contracted screen) and KNOWN GOOD (the rig saved and unchanged). The same lines are in STATE (commissioning), in COMMISSION STATUS on the wire, in the assistant's facts and as the Technician's 'Commission the rig' walkthrough, ticked by the same evidence." />

    <TextBlock Classes="h2" Text="KNOWN GOOD RIG" />
    <TextBlock Classes="label" Text="{Binding RigText}" TextWrapping="Wrap" LineHeight="20" />
    <WrapPanel Margin="0,6,0,0">
      <Button Classes="chip" Content="SAVE KNOWN GOOD" Command="{Binding SaveKnownGoodCommand}" Margin="0,0,8,6"
              ToolTip.Tip="The rig as it stands — this machine's Windows build, its GPUs and their drivers, every display with its mode and EDID, each screen's signal contract, the audio devices, the NDI senders, the control bindings and the render clock — saved beside the settings as the commissioned rig (RIG SAVE on the wire). Every boot compares the machine against it and says what moved" />
      <Button Classes="chip" Content="FORGET" Command="{Binding ForgetKnownGoodCommand}" Margin="0,0,8,6"
              ToolTip.Tip="Drops the saved rig; the comparison stops until the next SAVE KNOWN GOOD" />
    </WrapPanel>
    <TextBlock Classes="hint tip"
               Text="&quot;It worked yesterday&quot; answered before the outputs open. The commissioned rig is a file beside the settings (patterns.knowngood.json, in the support bundle); the super-check's RIG rows read the comparison — green unchanged, amber for a driver, a mode, a contract or a plan that moved, red for a display or an audio device that is gone — and every change of the comparison is one line in the journal. Nothing here decides anything: it is evidence for the engineer, the assistant and support." />

    <TextBlock Classes="h2" Text="STABILITY (WATCHDOG)" />
    <TextBlock Classes="label" Text="{Binding ScreensOwnedText}" TextWrapping="Wrap"
               ToolTip.Tip="Which process has this show folder's screens: this desk while its outputs are live, or the previous run when its windows are still playing" />
    <TextBlock Classes="label" Text="{Binding HealthText}" TextWrapping="Wrap" />
    <TextBlock Classes="label" Text="{Binding LastCrashText}" TextWrapping="Wrap" IsVisible="{Binding LastCrashVisible}"
               ToolTip.Tip="The note the watchdog left when it brought this run up: the exit code in words, when, after how long, and the mini-dump if one was written — also in patterns.log and the support bundle" />
    <TextBlock Classes="label" Text="{Binding DeskTickText}" TextWrapping="Wrap"
               ToolTip.Tip="The desk's once-a-second poll runs on the same thread as every click: what it costs, its worst minute and the area that took the time. Past 16 ms the desk skipped a frame; past 50 ms it stutters — the super-check's Desk tick row says the same. The poll runs in lanes — the critical areas first every tick, the steady ones after, and the housekeeping (the metrics, the remote's words, the Install page, the pickers, the web controls, the pages line) under a 4 ms budget in turn, what did not fit running first next tick — and the line reads that lane's p95 and the areas carried" />
    <TextBlock Classes="label" Text="{Binding SwitchText}" TextWrapping="Wrap"
               ToolTip.Tip="Every page switch is timed from the press to the first frame drawn: the worst of the last sixty says where its time went — building the page (only in the first seconds after a start, before the idle warm-up reached it), the handler, or the frame. Past 16 ms the desk skipped a frame; past 100 ms the operator felt it — the super-check's Page switch row says the same" />
    <TextBlock Classes="label" Text="{Binding SideEffectsText}" TextWrapping="Wrap"
               ToolTip.Tip="What the side effects after an edit cost: how many passes, the worst and the sections it followed, what the change mask let skip, and the systems that took the time" />
    <TextBlock Classes="label" Text="{Binding FilesText}" TextWrapping="Wrap"
               ToolTip.Tip="What the show's files cost: the autosave serialised and written on a worker from the frozen show (saves a newer one overtook are coalesced), the recovery record the same way, a show loaded and a cue sheet imported parsed on a worker — and whether any phase held the desk past a frame" />
    <TextBlock Classes="label" Text="{Binding PagesText}" TextWrapping="Wrap"
               ToolTip.Tip="The rail's pages are built one at a time in idle time after the start, the pages a show reaches for first — and only while the desk has the headroom: never while the stack is armed with the outputs live, never with a page switch in flight, not on a stressed tick. A small machine builds the likely pages alone and leaves the rest to the click that wants them" />
    <TextBlock Classes="label" Text="{Binding RenderBudgetText}" TextWrapping="Wrap"
               ToolTip.Tip="The engine's frame budget: every frame the preview, the monitors and the outputs draw is timed with the slowest stage inside it (the pattern, the layers, the overlays, the lower third, a crossfade). The worst frame of the last minute names the stage and the sink, so a slow frame says what to lower. Past 25 ms is a hitch the room can see; past 50 ms a stutter — the super-check's Render frame row and the RENDER tile say the same" />
    <TextBlock Classes="label" Text="{Binding GoText}" TextWrapping="Wrap"
               ToolTip.Tip="Every GO is timed from the press, through the publish the cue's steps made, to the first frame every output drew with it (the preview's when no output is open): the worst of the last sixty says whether the publish or the frame took the time. Past 50 ms a caller feels it; past 100 ms the room saw the cue land late — the super-check's GO to frame row says the same, and the glance line under the LIVE strip reads the last one" />
    <TextBlock Classes="label" Text="{Binding StartupText}" TextWrapping="Wrap"
               ToolTip.Tip="How long this start took to become a desk, phase by phase: the runtime and graphics set-up, the settings read, the services built, the window opened, the first frame drawn. A start past 8 s is one the operator notices; past 20 s it waited on a device, a disk or a network share, and the longest phase says which" />
    <TextBlock Classes="label" Text="{Binding RuntimeText}" TextWrapping="Wrap"
               ToolTip.Tip="The .NET runtime this build runs on, and the garbage collector's mode: while the outputs are live it runs in sustained low latency — collections happen in the background and a full stop-the-world collection is avoided for the length of the show; off air the interactive default comes back and memory is given back between shows" />
  </StackPanel>
</UserControl>
